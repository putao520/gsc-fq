# GSC-FQ Docker 环境配置示例

[server]
bind_ip = "0.0.0.0"

# Docker 环境下的代理配置
[[proxies]]
local_port = 33100
remote_host = "192.168.1.100"
remote_port = 8080
# source_ip = "172.17.0.1"  # Docker 网关IP，可选

[[proxies]]
local_port = 33200
remote_host = "192.168.1.101"
remote_port = 8080

[[proxies]]
local_port = 33300
remote_host = "192.168.1.102"
remote_port = 8080

# Docker Compose 环境变量支持示例
# 通过环境变量覆盖: PROXY_0_REMOTE_HOST=10.0.0.100